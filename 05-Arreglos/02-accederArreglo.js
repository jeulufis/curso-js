const autos = ['BMW', 'MERCEDEZ', 'VOLVO'];

const primerAuto = autos[0]; // BMW
const segundoAuto = autos[1]; // MERCEDEZ
const tercerAuto = auto[2]; // VOLVO

for (let i = 0; i < autos.length ; i++) {
   console.log(autos[i]);
}